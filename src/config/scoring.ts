/**
 * PLACEHOLDER — not implemented in Phase 0.
 *
 * Will hold TrendScore/RisingScore component weights, the log-scale robust
 * z-score normalisation parameters, and `SCORING_VERSION`, per
 * docs/IMPLEMENTATION_PLAN.md §16. Depends on Phase 1's baseline
 * measurements (median/MAD per platform) and Phase 6's analytics core —
 * not populated here.
 */
export {};

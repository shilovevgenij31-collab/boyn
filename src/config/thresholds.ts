/**
 * PLACEHOLDER — not implemented in Phase 0.
 *
 * Will hold the viral-candidate / post-tier thresholds from
 * docs/IMPLEMENTATION_PLAN.md §15 (VIRAL_QUALIFIED, EARLY_BREAKOUT, WATCH,
 * NOISE) once the PostTier domain type exists (Phase 6) and Phase 1's
 * provider spike has confirmed real field coverage. Intentionally left
 * without concrete values here so Phase 0 doesn't freeze unverified
 * assumptions into config.
 */
export {};

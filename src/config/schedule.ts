/**
 * PLACEHOLDER — not implemented in Phase 0.
 *
 * Will hold the collection schedule (discovery/refresh slot times in UTC,
 * per-tier slot allocation) from docs/IMPLEMENTATION_PLAN.md §12, consumed
 * by core/scheduling in Phase 5.
 */
export {};

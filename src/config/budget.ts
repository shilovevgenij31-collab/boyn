/**
 * PLACEHOLDER — not implemented in Phase 0.
 *
 * Will hold the FREE/LEAN/STANDARD crawl budget profiles (records per
 * discovery run, refresh allowance, per-provider monthly caps) from
 * docs/IMPLEMENTATION_PLAN.md §13, consumed by core/scheduling/budget.ts in
 * Phase 4. `BUDGET_PROFILE` / `BUDGET_MONTHLY_USD_MAX` already exist as env
 * vars (see src/config/env.ts) — this file will hold the profile
 * *definitions* those env vars select between, not select a value itself.
 */
export {};

# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env.*.local

# testing / coverage
/coverage

# Phase 1 provider spike: local raw output (may contain sanitized-but-still
# operational detail beyond what belongs in git). Committed outputs are
# docs/PROVIDER_SPIKE.md and test/fixtures/** only.
/.spike

# Vercel CLI local project link (org/project ids — not secret, but local
# machine state, not a repo concern)
.vercel

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# editor
.vscode/*
!.vscode/extensions.json
.idea
